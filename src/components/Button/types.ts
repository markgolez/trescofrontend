export enum ButtonColor {
  primary = '#007bff',
  secondary = '#6c757d',
  success = '#28a745',
  danger = '#dc3545',
}

export enum ButtonType {
  button = 'button',
  submit = 'submit',
  reset = 'reset',
}

export enum IconColor {
  white = '#fff',
  black = '#000',
} 